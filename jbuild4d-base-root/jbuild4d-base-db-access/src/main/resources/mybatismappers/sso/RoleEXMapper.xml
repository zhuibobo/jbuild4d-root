<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jbuild4d.base.dbaccess.dao.sso.RoleMapper">
    <select id="selectAll" resultMap="BaseResultMap">select<include refid="Base_Column_List"/> from TB4D_ROLE ORDER by  DESC</select>
    <delete id="deleteAll">delete from TB4D_ROLE</delete>
    <select id="nextOrderNum" resultType="integer">select case when max() is null then 1 else max()+1 end ORDERNUM from TB4D_ROLE</select>
    <select id="selectBySearch" parameterType="java.util.Map" resultMap="BaseResultMap">select<include refid="Base_Column_List"/>from TB4D_ROLE<where>
        <if test="roleName !=null and roleName !=''"> and ROLE_NAME like #{roleName} </if>
    </where>ORDER by  DESC</select>
    <select id="selectLessThanRecord" parameterType="java.lang.String" resultMap="BaseResultMap"><![CDATA[select * from TB4D_ROLE
                                                                                                          where  = (select max() from TB4D_ROLE where <(select  from TB4D_ROLE where ROLE_ID=#{Id,jdbcType=NVARCHAR}))]]></select>
    <select id="selectGreaterThanRecord" parameterType="java.lang.String" resultMap="BaseResultMap"><![CDATA[select * from TB4D_ROLE
                                                                                                             where  = (select min() from TB4D_ROLE where >(select  from TB4D_ROLE where ROLE_ID=#{Id,jdbcType=NVARCHAR}))]]></select>
</mapper>
